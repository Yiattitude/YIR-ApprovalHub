# 审批系统 - 前端开发文档

本篇文档面向前端开发者、测试工程师以及对接的后端同学，目标是在不阅读源码的情况下理解项目采用的技术栈、运行方式以及每一个文件存在的理由。全文以文字说明为主，避免粘贴大段代码，方便在评审或交接场景中快速定位问题。

## 1. 技术与运行基线

### 1.1 核心技术选型
- **React 18 + TypeScript 5**：全部页面均为函数式组件，启用严格的类型检查，保证组件签名清晰。
- **Vite 5**：提供快速的开发服务器与构建能力，并负责 `.env` 注入、TS 编译与资源打包。
- **Tailwind CSS + shadcn/ui**：Tailwind 负责设计系统的原子样式，shadcn/ui 则为常用交互提供可复用的无样式基座。
- **Zustand**：管理认证等全局状态，体积小且无需冗余模板代码。
- **Axios + 自封装 request.ts**：统一请求拦截、鉴权头与错误提示。

### 1.2 构建与运行方式
- 推荐使用 Node.js 18+ 与 npm 9+。`npm run dev` 启动 Vite 开发服务器，`npm run build` 触发 TypeScript 编译与产物打包，`npm run preview` 在本地预览构建结果，`npm run lint` 调用 ESLint 与 TypeScript 插件进行质量门禁。
- 构建产物输出至 `dist/`，通过反向代理或对象存储静态托管，所有 API 调用在构建时依赖 `VITE_APP_BASE_API` 前缀。
- `.env.development` 与 `.env.production` 控制基础 API 地址与系统标题。新的环境变量应以 `VITE_` 前缀命名，以便被 Vite 注入。

### 1.3 运行阶段的关键约束
- 任何对权限、会话或主题的修改都应保存在 Zustand store 中，以保证路由切换后仍然有效。
- 组件样式需依赖 Tailwind 设计 Token（`--background`、`--primary` 等）；如需新增主题色，必须同步更新 `tailwind.config.js` 与 `globals.css` 中的 CSS 变量。
- API 模块仅暴露纯函数，不直接处理 UI，便于在 React Query 或其他数据层中复用。

## 2. 顶层目录与配置文件说明

| 文件/目录 | 职责说明 |
| --- | --- |
| [YIR-ApprovalHub/frontend/package.json](YIR-ApprovalHub/frontend/package.json) | 记录依赖、脚本与项目基本信息，是安装与 CI 的入口。 |
| [YIR-ApprovalHub/frontend/package-lock.json](YIR-ApprovalHub/frontend/package-lock.json) | 锁定依赖版本，确保团队成员构建结果一致。 |
| [YIR-ApprovalHub/frontend/tsconfig.json](YIR-ApprovalHub/frontend/tsconfig.json) | 定义 TypeScript 编译选项、路径别名等全局规则。 |
| [YIR-ApprovalHub/frontend/tsconfig.node.json](YIR-ApprovalHub/frontend/tsconfig.node.json) | 供 Vite、ESLint 等 Node 侧工具引用的 TS 配置，避免与浏览器端配置混淆。 |
| [YIR-ApprovalHub/frontend/vite.config.ts](YIR-ApprovalHub/frontend/vite.config.ts) | 设定 Vite 插件、路径别名 `@`、代理 `/api` 以及构建分包策略。 |
| [YIR-ApprovalHub/frontend/tailwind.config.js](YIR-ApprovalHub/frontend/tailwind.config.js) | 维护 Tailwind 的扫描范围、主题扩展、动画与插件列表。 |
| [YIR-ApprovalHub/frontend/postcss.config.js](YIR-ApprovalHub/frontend/postcss.config.js) | 配置 PostCSS、Autoprefixer 与 Tailwind 插件执行顺序。 |
| [YIR-ApprovalHub/frontend/components.json](YIR-ApprovalHub/frontend/components.json) | shadcn/ui 组件生成器的元数据，记录已经拉取的组件及动效。 |
| [YIR-ApprovalHub/frontend/index.html](YIR-ApprovalHub/frontend/index.html) | 单页应用模板，挂载点 `#root` 与基本 meta 均在此声明。 |
| [YIR-ApprovalHub/frontend/.env.development](YIR-ApprovalHub/frontend/.env.development) / [.env.production](YIR-ApprovalHub/frontend/.env.production) | 维护不同环境下的 API 前缀与标题，Vite 会在启动时注入。 |
| [YIR-ApprovalHub/frontend/.gitignore](YIR-ApprovalHub/frontend/.gitignore) | 统一忽略规则，防止临时文件与构建产物进入版本库。 |
| [YIR-ApprovalHub/frontend/public](YIR-ApprovalHub/frontend/public) | Vite 原生静态目录，目前包含 `sakura.js`（樱花飘落效果脚本）与 `wallpaper.jpg`（登录背景图）。这些资源在构建时原样拷贝。 |
| [YIR-ApprovalHub/frontend/dist](YIR-ApprovalHub/frontend/dist) | 构建输出目录，仅在发布或排查产物差异时查看，不纳入版本控制。 |
| [YIR-ApprovalHub/frontend/node_modules](YIR-ApprovalHub/frontend/node_modules) | npm 依赖缓存目录；如需升级依赖，请使用 `npm install 包名@版本` 并提交 lock 文件。 |
| [YIR-ApprovalHub/frontend/README.md](YIR-ApprovalHub/frontend/README.md) | 前端子项目自述文件，记录开发者快速上手指引。 |
| [YIR-ApprovalHub/frontend/src](YIR-ApprovalHub/frontend/src) | 所有业务源码所在目录，下文逐一说明各文件职责。 |

## 3. 源码结构概览

### 3.1 入口与公共资源

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/main.tsx](YIR-ApprovalHub/frontend/src/main.tsx) | React 应用真正的入口，挂载 `App` 并注入全局 Provider（如路由或状态）。 |
| [YIR-ApprovalHub/frontend/src/App.tsx](YIR-ApprovalHub/frontend/src/App.tsx) | 顶层路由与布局调度中心，整合 ShellLayout、页面权限判断与全局 UI。 |
| [YIR-ApprovalHub/frontend/src/vite-env.d.ts](YIR-ApprovalHub/frontend/src/vite-env.d.ts) | 声明 Vite 注入的类型，避免在 TypeScript 中访问 `import.meta.env` 时报错。 |
| [YIR-ApprovalHub/frontend/src/public/logo.png](YIR-ApprovalHub/frontend/src/public/logo.png) | 内嵌于多处页面的品牌 Logo，与 `public/` 中的大背景区分开管理。 |

### 3.2 API 模块

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/api/index.ts](YIR-ApprovalHub/frontend/src/api/index.ts) | 汇总所有后端接口的请求方法与类型声明，是页面调用 API 的唯一入口。 |
| [YIR-ApprovalHub/frontend/src/api/admin.ts](YIR-ApprovalHub/frontend/src/api/admin.ts) | 定义管理员侧的请求封装，如用户/部门/岗位维护、统计报表导出等。 |

### 3.3 常量与枚举

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/constants/application.ts](YIR-ApprovalHub/frontend/src/constants/application.ts) | 统一维护申请类型、状态标签、颜色映射等常量，保持前后端语义一致。 |

### 3.4 工具库

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/lib/request.ts](YIR-ApprovalHub/frontend/src/lib/request.ts) | 对 Axios 进行包装，集中处理 Token 注入、错误提示、全局 Loading 等横切逻辑。 |
| [YIR-ApprovalHub/frontend/src/lib/utils.ts](YIR-ApprovalHub/frontend/src/lib/utils.ts) | 存放 `cn` 合并类名、日期格式化等纯函数，避免在组件中重复实现。 |

### 3.5 状态管理

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/store/authStore.ts](YIR-ApprovalHub/frontend/src/store/authStore.ts) | 基于 Zustand 保存当前登录用户、Token、角色与便捷操作（登录、登出、刷新个人信息）。 |

### 3.6 样式与静态资源

| 文件/目录 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/styles/globals.css](YIR-ApprovalHub/frontend/src/styles/globals.css) | 全局样式入口，负责注入 Tailwind 指令与主题变量，是设计系统的底座。 |
| [YIR-ApprovalHub/frontend/public](YIR-ApprovalHub/frontend/public) | 登录与展示页使用的静态脚本/图片，部署后可被直接访问。 |
| [YIR-ApprovalHub/frontend/src/public](YIR-ApprovalHub/frontend/src/public) | 放置需通过打包器参与依赖图的小型资源，目前仅有 `logo.png`。 |

## 4. 复用组件与页面

### 4.1 复用组件（业务能力）

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/components/ApplicationDetailDialog.tsx](YIR-ApprovalHub/frontend/src/components/ApplicationDetailDialog.tsx) | 弹窗形式展示单条申请的字段、审批轨迹与附件，供管理员和审批人复核使用。 |
| [YIR-ApprovalHub/frontend/src/components/AttachmentUpload.tsx](YIR-ApprovalHub/frontend/src/components/AttachmentUpload.tsx) | 统一的附件上传与预览控件，内置大小/类型校验并与后端文件接口对接。 |
| [YIR-ApprovalHub/frontend/src/components/PaginationControls.tsx](YIR-ApprovalHub/frontend/src/components/PaginationControls.tsx) | 列表分页的通用控制条，兼容待办列表、申请列表等场景。 |
| [YIR-ApprovalHub/frontend/src/components/RoleBasedDashboard.tsx](YIR-ApprovalHub/frontend/src/components/RoleBasedDashboard.tsx) | 根据当前角色切换展示 KPI、图表与待办提示，供多个仪表盘复用。 |

### 4.2 布局组件

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/components/layout/ShellLayout.tsx](YIR-ApprovalHub/frontend/src/components/layout/ShellLayout.tsx) | 定义统一的导航、侧栏、面包屑与内容容器，同时托管主题切换与响应式折叠逻辑。 |

### 4.3 UI Primitives（shadcn/ui 定制件）

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/components/ui/badge.tsx](YIR-ApprovalHub/frontend/src/components/ui/badge.tsx) | 渲染状态标记，小尺寸的颜色标签。 |
| [YIR-ApprovalHub/frontend/src/components/ui/button.tsx](YIR-ApprovalHub/frontend/src/components/ui/button.tsx) | 统一按钮样式，支持主/次/危险等多种变体。 |
| [YIR-ApprovalHub/frontend/src/components/ui/card.tsx](YIR-ApprovalHub/frontend/src/components/ui/card.tsx) | 卡片容器，提供 Header / Content / Footer 分区。 |
| [YIR-ApprovalHub/frontend/src/components/ui/checkbox.tsx](YIR-ApprovalHub/frontend/src/components/ui/checkbox.tsx) | 封装 Radix Checkbox，并兼容表单校验。 |
| [YIR-ApprovalHub/frontend/src/components/ui/dialog.tsx](YIR-ApprovalHub/frontend/src/components/ui/dialog.tsx) | 弹窗基础件，结合遮罩、标题与操作区。 |
| [YIR-ApprovalHub/frontend/src/components/ui/input.tsx](YIR-ApprovalHub/frontend/src/components/ui/input.tsx) | 文本输入框，含禁用、错误态等状态。 |
| [YIR-ApprovalHub/frontend/src/components/ui/label.tsx](YIR-ApprovalHub/frontend/src/components/ui/label.tsx) | 表单标签组件，承载辅助说明。 |
| [YIR-ApprovalHub/frontend/src/components/ui/radio-group.tsx](YIR-ApprovalHub/frontend/src/components/ui/radio-group.tsx) | 单选按钮组，支持键盘可访问性。 |
| [YIR-ApprovalHub/frontend/src/components/ui/select.tsx](YIR-ApprovalHub/frontend/src/components/ui/select.tsx) | 下拉选择器，常用于审批人选择、类型筛选。 |
| [YIR-ApprovalHub/frontend/src/components/ui/table.tsx](YIR-ApprovalHub/frontend/src/components/ui/table.tsx) | 表格骨架，统一表头、行样式与滚动行为。 |
| [YIR-ApprovalHub/frontend/src/components/ui/textarea.tsx](YIR-ApprovalHub/frontend/src/components/ui/textarea.tsx) | 多行输入框，供填写审批意见或申请说明。 |

## 5. 页面层文件说明

### 5.1 顶层页面

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/pages/Dashboard.tsx](YIR-ApprovalHub/frontend/src/pages/Dashboard.tsx) | 根据用户身份呈现综合仪表盘，聚合待办、统计与公告。 |
| [YIR-ApprovalHub/frontend/src/pages/Login.tsx](YIR-ApprovalHub/frontend/src/pages/Login.tsx) | 登录表单、验证码与 remember me 逻辑，并在成功后调用 `authStore`。 |
| [YIR-ApprovalHub/frontend/src/pages/Register.tsx](YIR-ApprovalHub/frontend/src/pages/Register.tsx) | 新员工注册入口，包含基本信息录入与后台校验。 |
| [YIR-ApprovalHub/frontend/src/pages/MyApplications.tsx](YIR-ApprovalHub/frontend/src/pages/MyApplications.tsx) | 普通员工查看自己发起的申请，支持筛选、撤回与详情弹窗。 |

### 5.2 管理员模块

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/pages/Admin/AdminDashboard.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/AdminDashboard.tsx) | 管理员专属仪表盘，展示审批总览、风控提醒与关键 KPI。 |
| [YIR-ApprovalHub/frontend/src/pages/Admin/AllApplications.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/AllApplications.tsx) | 全量申请列表，提供跨部门检索、导出与强制流转操作。 |
| [YIR-ApprovalHub/frontend/src/pages/Admin/DeptManagement.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/DeptManagement.tsx) | 部门树维护、负责人设置与启停控制。 |
| [YIR-ApprovalHub/frontend/src/pages/Admin/PostManagement.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/PostManagement.tsx) | 岗位定义、审批阈值、继任者指派等功能。 |
| [YIR-ApprovalHub/frontend/src/pages/Admin/PostAssignment.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/PostAssignment.tsx) | 将岗位与具体人员绑定，保证审批链条完整。 |
| [YIR-ApprovalHub/frontend/src/pages/Admin/RoleAssignment.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/RoleAssignment.tsx) | 管理角色-权限映射，控制菜单与 API 访问。 |
| [YIR-ApprovalHub/frontend/src/pages/Admin/UserManagement.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/UserManagement.tsx) | 管理用户生命周期，包括创建、禁用、密码重置。 |
| [YIR-ApprovalHub/frontend/src/pages/Admin/ReportCenter.tsx](YIR-ApprovalHub/frontend/src/pages/Admin/ReportCenter.tsx) | 汇总统计报表、导出 CSV/Excel，并支持部门维度钻取。 |

### 5.3 审批人模块

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/pages/Approver/ApproverDashboard.tsx](YIR-ApprovalHub/frontend/src/pages/Approver/ApproverDashboard.tsx) | 审批人概览页，展示近七日/三十日处理量、通过率与 SLA 告警。 |
| [YIR-ApprovalHub/frontend/src/pages/Approver/TodoTasks.tsx](YIR-ApprovalHub/frontend/src/pages/Approver/TodoTasks.tsx) | 待办任务队列，支持批量操作、关键词检索与 SLA 倒计时。 |
| [YIR-ApprovalHub/frontend/src/pages/Approver/DoneTasks.tsx](YIR-ApprovalHub/frontend/src/pages/Approver/DoneTasks.tsx) | 已办任务记录，便于追溯审批意见与附件。 |
| [YIR-ApprovalHub/frontend/src/pages/Approver/ApproverSummary.tsx](YIR-ApprovalHub/frontend/src/pages/Approver/ApproverSummary.tsx) | 汇总报表，按申请类型、部门或时间切片展示工作量。 |

### 5.4 普通员工模块

| 文件 | 作用 |
| --- | --- |
| [YIR-ApprovalHub/frontend/src/pages/User/UserDashboard.tsx](YIR-ApprovalHub/frontend/src/pages/User/UserDashboard.tsx) | 员工专属仪表盘，展示个人 KPI、请假额度与报销概览。 |
| [YIR-ApprovalHub/frontend/src/pages/User/UserSummary.tsx](YIR-ApprovalHub/frontend/src/pages/User/UserSummary.tsx) | 个人统计详情，提供时间范围筛选与类型拆分。 |
| [YIR-ApprovalHub/frontend/src/pages/User/CreateLeave.tsx](YIR-ApprovalHub/frontend/src/pages/User/CreateLeave.tsx) | 请假申请表单，集成类型选择、时间段与审批人选择器。 |
| [YIR-ApprovalHub/frontend/src/pages/User/CreateReimburse.tsx](YIR-ApprovalHub/frontend/src/pages/User/CreateReimburse.tsx) | 报销申请表单，包含金额校验与发票附件上传。 |
| [YIR-ApprovalHub/frontend/src/pages/User/ApprovalHistory.tsx](YIR-ApprovalHub/frontend/src/pages/User/ApprovalHistory.tsx) | 员工视角的审批历史列表，可查看每一步流转与意见。 |

## 6. 数据流与协同模式
- **请求流**：页面组件通过调用 `api` 模块获取数据，`api` 模块依赖 `lib/request.ts` 注入 Token 与错误处理。若调用涉及登录态，`authStore` 会在响应后更新本地状态并触发界面刷新。
- **状态同步**：除认证信息外，其余大部分数据在组件内部管理或通过 React Query 缓存；当某个操作会影响其他页面时（例如管理员更换岗位），统一触发刷新事件或调用相应的 API 再次拉取列表。
- **权限控制**：`App.tsx` 中读取 `authStore`，结合路由元信息决定是否允许访问某个页面，并在 `ShellLayout` 中决定菜单可见性。
- **样式与主题**：`globals.css` 里的 CSS 变量由 `ShellLayout` 根据系统主题切换，Tailwind 类名仅引用变量而不写死颜色，确保暗色模式与品牌定制的可扩展性。

## 7. 开发约定
- 新增页面或组件时，务必同步在本文的对应表格中补充描述，保证交接资料完整。
- API 模块仅返回 Promise，不在内部进行 UI 提示；统一交由调用方或通用的 toast 机制处理。
- 表单类页面统一使用组件内的 shadcn/ui + Tailwind 方案，避免在全局样式中编写特殊选择器。
- 当需要新增全局状态时，优先考虑扩展 `authStore` 或建立新的 store 模块，避免将状态塞入随机组件。
- 静态资源若需在构建期内被引用（如通过 `import logo from '@/public/logo.png'`），应放入 `src/public/`；若仅需按 URL 访问，则放入 `public/`。

## 8. 文档信息
- **版本**：v1.1
- **最后更新**：2026-01-20
- **维护人**：前端负责人

如需了解 API 协议或后端实现，请查阅 [docs/06-API接口文档.md](docs/06-API接口文档.md) 与 [docs/04-后端开发文档.md](docs/04-后端开发文档.md)。# 审批系统 - 前端开发文档

## 1. 技术栈

### 1.1 核心框架
- **React**：18.x+（使用 Hooks 和函数组件）
- **TypeScript**：5.0+
- **Vite**：5.0+（构建工具）

### 1.2 UI 与样式
- **shadcn/ui**：基于 Radix UI 的组件库
- **Tailwind CSS**：原子化 CSS 框架
- **Radix UI**：无样式组件库
- **图标**：lucide-react

### 1.3 状态管理与路由
- **Zustand**：轻量级状态管理（推荐）
- **React Context**：内置状态管理
- **React Router**：路由管理

### 1.4 HTTP 与数据处理
- **Axios**：HTTP 请求库
- **TanStack Query (React Query)**：数据获取和缓存
- **React Hook Form**：表单处理
- **Zod**：TypeScript 优先的模式验证

### 1.5 工具库
- **Day.js**：日期处理
- **clsx / cn**：条件样式合并
- **date-fns**：日期工具库

### 1.6 代码规范
- **ESLint**：代码检查
- **Prettier**：代码格式化

### 1.7 选做功能（流程设计器）
- **bpmn-js**：流程设计器
- **React Flow**：流程可视化（替代方案）

## 2. 项目结构

```
frontend/
├── public/                              # 静态资源
│   └── favicon.ico
├── src/
│   ├── api/                             # API 接口
│   │   ├── auth.ts                      # 认证相关
│   │   ├── user.ts                      # 用户管理
│   │   ├── dept.ts                      # 部门管理
│   │   ├── application.ts               # 申请管理
│   │   ├── task.ts                      # 任务管理
│   │   └── file.ts                      # 文件管理
│   ├── assets/                          # 资源文件
│   │   └── images/                      # 图片
│   ├── components/                      # 组件
│   │   ├── ui/                          # shadcn/ui 组件
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── form.tsx
│   │   │   ├── input.tsx
│   │   │   ├── select.tsx
│   │   │   ├── table.tsx
│   │   │   ├── toast.tsx
│   │   │   └── ...
│   │   ├── layout/                      # 布局组件
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   ├── MainLayout.tsx
│   │   │   └── AuthLayout.tsx
│   │   ├── ApprovalHistory/             # 审批历史组件
│   │   │   └── ApprovalHistory.tsx
│   │   ├── FileUpload/                  # 文件上传组件
│   │   │   └── FileUpload.tsx
│   │   └── StatusBadge/                 # 状态徽章组件
│   │       └── StatusBadge.tsx
│   ├── hooks/                           # 自定义 Hooks
│   │   ├── useAuth.ts                   # 认证 Hook
│   │   ├── useApplication.ts            # 申请相关 Hook
│   │   └── useToast.ts                  # Toast Hook
│   ├── lib/                             # 工具库
│   │   ├── utils.ts                     # 通用工具函数
│   │   ├── request.ts                   # Axios 封装
│   │   └── cn.ts                        # className 合并工具
│   ├── pages/                           # 页面组件
│   │   ├── Login/                       # 登录页
│   │   │   └── Login.tsx
│   │   ├── Dashboard/                   # 首页
│   │   │   └── Dashboard.tsx
│   │   ├── Application/                 # 申请管理
│   │   │   ├── ApplicationList.tsx      # 申请列表
│   │   │   ├── ApplicationCreate.tsx    # 创建申请
│   │   │   ├── ApplicationDetail.tsx    # 申请详情
│   │   │   ├── LeaveForm.tsx           # 请假表单
│   │   │   └── ReimburseForm.tsx       # 报销表单
│   │   ├── Task/                        # 任务管理
│   │   │   ├── TodoList.tsx            # 待办列表
│   │   │   ├── DoneList.tsx            # 已办列表
│   │   │   └── ApprovalPage.tsx        # 审批页面
│   │   └── System/                      # 系统管理
│   │       ├── UserManagement.tsx       # 用户管理
│   │       ├── DeptManagement.tsx       # 部门管理
│   │       └── RoleManagement.tsx       # 角色管理
│   ├── router/                          # 路由配置
│   │   ├── index.tsx                    # 路由入口
│   │   └── ProtectedRoute.tsx           # 路由守卫
│   ├── store/                           # Zustand 状态管理
│   │   ├── authStore.ts                 # 认证状态
│   │   ├── appStore.ts                  # 应用状态
│   │   └── userStore.ts                 # 用户状态
│   ├── types/                           # TypeScript 类型定义
│   │   ├── api.ts                       # API 类型
│   │   ├── user.ts                      # 用户类型
│   │   ├── application.ts               # 申请类型
│   │   └── index.ts                     # 全局类型
│   ├── styles/                          # 样式文件
│   │   └── globals.css                  # 全局样式（含 Tailwind）
│   ├── App.tsx                          # 根组件
│   ├── main.tsx                         # 入口文件
│   └── vite-env.d.ts                    # Vite 环境变量类型
├── .env.development                     # 开发环境变量
├── .env.production                      # 生产环境变量
├── .eslintrc.cjs                        # ESLint 配置
├── .prettierrc                          # Prettier 配置
├── components.json                      # shadcn/ui 配置
├── index.html                           # HTML 入口
├── package.json                         # 依赖配置
├── postcss.config.js                    # PostCSS 配置
├── tailwind.config.js                   # Tailwind 配置
├── tsconfig.json                        # TypeScript 配置
├── vite.config.ts                       # Vite 配置
└── README.md                            # 项目说明
```

## 3. 环境配置

### 3.1 package.json

```json
{
  "name": "approval-system-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.21.0",
    "@tanstack/react-query": "^5.17.0",
    "zustand": "^4.4.7",
    "axios": "^1.6.5",
    "react-hook-form": "^7.49.2",
    "zod": "^3.22.4",
    "@hookform/resolvers": "^3.3.3",
    "dayjs": "^1.11.10",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.0",
    "@radix-ui/react-dialog": "^1.0.5",
    "@radix-ui/react-dropdown-menu": "^2.0.6",
    "@radix-ui/react-label": "^2.0.2",
    "@radix-ui/react-select": "^2.0.0",
    "@radix-ui/react-slot": "^1.0.2",
    "@radix-ui/react-toast": "^1.1.5",
    "lucide-react": "^0.303.0",
    "class-variance-authority": "^0.7.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.46",
    "@types/react-dom": "^18.2.18",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "eslint": "^8.56.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0",
    "prettier": "^3.1.1",
    "prettier-plugin-tailwindcss": "^0.5.10"
  }
}
```

### 3.2 vite.config.ts

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src')
    }
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom', 'react-router-dom'],
          'ui-vendor': ['@radix-ui/react-dialog', '@radix-ui/react-dropdown-menu']
        }
      }
    }
  }
})
```

### 3.3 tailwind.config.js

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ['class'],
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}'
  ],
  theme: {
    container: {
      center: true,
      padding: '2rem',
      screens: {
        '2xl': '1400px'
      }
    },
    extend: {
      colors: {
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))'
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))'
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))'
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))'
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))'
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))'
        },
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))'
        }
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)'
      },
      keyframes: {
        'accordion-down': {
          from: { height: 0 },
          to: { height: 'var(--radix-accordion-content-height)' }
        },
        'accordion-up': {
          from: { height: 'var(--radix-accordion-content-height)' },
          to: { height: 0 }
        }
      },
      animation: {
        'accordion-down': 'accordion-down 0.2s ease-out',
        'accordion-up': 'accordion-up 0.2s ease-out'
      }
    }
  },
  plugins: [require('tailwindcss-animate')]
}
```

### 3.4 src/styles/globals.css

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --secondary: 210 40% 96.1%;
    --secondary-foreground: 222.2 47.4% 11.2%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --accent: 210 40% 96.1%;
    --accent-foreground: 222.2 47.4% 11.2%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 222.2 84% 4.9%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

### 3.5 环境变量

**.env.development**
```bash
VITE_APP_BASE_API=/api
VITE_APP_TITLE=审批系统
```

**.env.production**
```bash
VITE_APP_BASE_API=https://api.yourcompany.com
VITE_APP_TITLE=审批系统
```

## 4. 核心代码实现

### 4.1 Axios 封装 (lib/request.ts)

```typescript
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { toast } from '@/components/ui/use-toast'

// Token 管理
const TOKEN_KEY = 'ACCESS_TOKEN'

export const getToken = (): string | null => {
  return localStorage.getItem(TOKEN_KEY)
}

export const setToken = (token: string): void => {
  localStorage.setItem(TOKEN_KEY, token)
}

export const removeToken = (): void => {
  localStorage.removeItem(TOKEN_KEY)
}

// 创建 Axios 实例
const request: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_APP_BASE_API,
  timeout: 15000,
  headers: {
    'Content-Type': 'application/json;charset=UTF-8'
  }
})

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    const token = getToken()
    if (token && config.headers) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response: AxiosResponse) => {
    const res = response.data
    
    if (res.code !== 200) {
      toast({
        variant: 'destructive',
        title: '错误',
        description: res.message || '请求失败'
      })
      
      if (res.code === 401) {
        removeToken()
        window.location.href = '/login'
      }
      
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    
    return res
  },
  (error) => {
    toast({
      variant: 'destructive',
      title: '网络错误',
      description: error.message || '网络连接失败'
    })
    return Promise.reject(error)
  }
)

export default request
```

### 4.2 工具函数 (lib/utils.ts)

```typescript
import { type ClassValue, clsx } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

### 4.3 类型定义 (types/application.ts)

```typescript
export interface Application {
  appId: number
  appNo: string
  appType: string
  title: string
  applicantId: number
  applicantName: string
  deptName: string
  status: number
  currentNode?: string
  submitTime: string
  finishTime?: string
}

export interface LeaveApplication {
  leaveType: number
  startTime: string
  endTime: string
  days: number
  reason: string
  attachment?: string
}

export interface ReimburseApplication {
  expenseType: number
  amount: number
  reason: string
  invoiceAttachment: string
  occurDate: string
}

export interface ApprovalTask {
  taskId: number
  appId: number
  nodeName: string
  assigneeId: number
  assigneeName: string
  taskStatus: number
  createTime: string
}

export interface ApprovalHistory {
  historyId: number
  appId: number
  nodeName: string
  approverName: string
  action: number
  comment: string
  approveTime: string
}
```

### 4.4 API 接口 (api/application.ts)

```typescript
import request from '@/lib/request'
import type { Application, LeaveApplication, ReimburseApplication } from '@/types/application'

/**
 * 创建请假申请
 */
export const createLeaveApplication = (data: LeaveApplication) => {
  return request({
    url: '/application/leave',
    method: 'post',
    data
  })
}

/**
 * 创建报销申请
 */
export const createReimburseApplication = (data: ReimburseApplication) => {
  return request({
    url: '/application/reimburse',
    method: 'post',
    data
  })
}

/**
 * 查询我的申请列表
 */
export const getMyApplications = (params?: any) => {
  return request({
    url: '/application/my',
    method: 'get',
    params
  })
}

/**
 * 查询申请详情
 */
export const getApplicationDetail = (appId: number) => {
  return request({
    url: `/application/${appId}`,
    method: 'get'
  })
}

/**
 * 撤回申请
 */
export const withdrawApplication = (appId: number) => {
  return request({
    url: `/application/withdraw/${appId}`,
    method: 'put'
  })
}
```

### 4.5 Zustand Store (store/authStore.ts)

```typescript
import { create } from 'zustand'
import { login as loginApi, getUserInfo as getUserInfoApi } from '@/api/auth'
import { setToken, removeToken } from '@/lib/request'

interface User {
  userId: number
  username: string
  realName: string
  roles: string[]
}

interface AuthState {
  token: string
  user: User | null
  login: (username: string, password: string) => Promise<void>
  getUserInfo: () => Promise<void>
  logout: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  token: '',
  user: null,
  
  login: async (username: string, password: string) => {
    const res = await loginApi({ username, password })
    const token = res.data.token
    setToken(token)
    set({ token })
  },
  
  getUserInfo: async () => {
    const res = await getUserInfoApi()
    set({ user: res.data })
  },
  
  logout: () => {
    removeToken()
    set({ token: '', user: null })
  }
}))
```

### 4.6 路由配置 (router/index.tsx)

```typescript
import { createBrowserRouter, Navigate } from 'react-router-dom'
import ProtectedRoute from './ProtectedRoute'
import MainLayout from '@/components/layout/MainLayout'
import Login from '@/pages/Login/Login'
import Dashboard from '@/pages/Dashboard/Dashboard'
import ApplicationList from '@/pages/Application/ApplicationList'
import ApplicationCreate from '@/pages/Application/ApplicationCreate'
import TodoList from '@/pages/Task/TodoList'
import DoneList from '@/pages/Task/DoneList'

export const router = createBrowserRouter([
  {
    path: '/login',
    element: <Login />
  },
  {
    path: '/',
    element: (
      <ProtectedRoute>
        <MainLayout />
      </ProtectedRoute>
    ),
    children: [
      {
        index: true,
        element: <Navigate to="/dashboard" replace />
      },
      {
        path: 'dashboard',
        element: <Dashboard />
      },
      {
        path: 'application',
        children: [
          {
            path: 'list',
            element: <ApplicationList />
          },
          {
            path: 'create',
            element: <ApplicationCreate />
          }
        ]
      },
      {
        path: 'task',
        children: [
          {
            path: 'todo',
            element: <TodoList />
          },
          {
            path: 'done',
            element: <DoneList />
          }
        ]
      }
    ]
  }
])
```

### 4.7 路由守卫 (router/ProtectedRoute.tsx)

```typescript
import { Navigate } from 'react-router-dom'
import { getToken } from '@/lib/request'

interface ProtectedRouteProps {
  children: React.ReactNode
}

export default function ProtectedRoute({ children }: ProtectedRouteProps) {
  const token = getToken()
  
  if (!token) {
    return <Navigate to="/login" replace />
  }
  
  return <>{children}</>
}
```

### 4.8 页面示例 - 登录页 (pages/Login/Login.tsx)

```typescript
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'
import { useAuthStore } from '@/store/authStore'
import { Button } from '@/components/ui/button'
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage
} from '@/components/ui/form'
import { Input } from '@/components/ui/input'
import { useToast } from '@/components/ui/use-toast'

const formSchema = z.object({
  username: z.string().min(1, '请输入用户名'),
  password: z.string().min(1, '请输入密码')
})

export default function Login() {
  const navigate = useNavigate()
  const { toast } = useToast()
  const { login } = useAuthStore()
  const [loading, setLoading] = useState(false)

  const form = useForm<z.infer<typeof formSchema>>({
    resolver: zodResolver(formSchema),
    defaultValues: {
      username: 'admin',
      password: '123456'
    }
  })

  const onSubmit = async (values: z.infer<typeof formSchema>) => {
    setLoading(true)
    try {
      await login(values.username, values.password)
      toast({
        title: '登录成功',
        description: '欢迎回来！'
      })
      navigate('/')
    } catch (error) {
      console.error(error)
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className="flex min-h-screen items-center justify-center bg-gradient-to-br from-blue-600 to-purple-700">
      <div className="w-full max-w-md rounded-lg bg-white p-8 shadow-2xl">
        <h1 className="mb-6 text-center text-3xl font-bold text-gray-800">
          审批系统
        </h1>
        
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
            <FormField
              control={form.control}
              name="username"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>用户名</FormLabel>
                  <FormControl>
                    <Input placeholder="请输入用户名" {...field} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            
            <FormField
              control={form.control}
              name="password"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>密码</FormLabel>
                  <FormControl>
                    <Input type="password" placeholder="请输入密码" {...field} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            
            <Button type="submit" className="w-full" disabled={loading}>
              {loading ? '登录中...' : '登录'}
            </Button>
          </form>
        </Form>
      </div>
    </div>
  )
}
```

### 4.9 页面示例 - 请假申请表单 (pages/Application/LeaveForm.tsx)

```typescript
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'
import { useNavigate } from 'react-router-dom'
import { createLeaveApplication } from '@/api/application'
import { Button } from '@/components/ui/button'
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage
} from '@/components/ui/form'
import { Input } from '@/components/ui/input'
import { Textarea } from '@/components/ui/textarea'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue
} from '@/components/ui/select'
import { useToast } from '@/components/ui/use-toast'

const formSchema = z.object({
  leaveType: z.number().min(1, '请选择请假类型'),
  startTime: z.string().min(1, '请选择开始时间'),
  endTime: z.string().min(1, '请选择结束时间'),
  days: z.number().min(0.5, '请假天数至少0.5天'),
  reason: z.string().min(1, '请输入请假事由'),
  attachment: z.string().optional()
})

export default function LeaveForm() {
  const navigate = useNavigate()
  const { toast } = useToast()

  const form = useForm<z.infer<typeof formSchema>>({
    resolver: zodResolver(formSchema),
    defaultValues: {
      leaveType: undefined,
      startTime: '',
      endTime: '',
      days: 1,
      reason: '',
      attachment: ''
    }
  })

  const onSubmit = async (values: z.infer<typeof formSchema>) => {
    try {
      await createLeaveApplication(values)
      toast({
        title: '提交成功',
        description: '您的请假申请已提交'
      })
      navigate('/application/list')
    } catch (error) {
      console.error(error)
    }
  }

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
        <FormField
          control={form.control}
          name="leaveType"
          render={({ field }) => (
            <FormItem>
              <FormLabel>请假类型</FormLabel>
              <Select
                onValueChange={(value) => field.onChange(Number(value))}
                defaultValue={field.value?.toString()}
              >
                <FormControl>
                  <SelectTrigger>
                    <SelectValue placeholder="请选择请假类型" />
                  </SelectTrigger>
                </FormControl>
                <SelectContent>
                  <SelectItem value="1">事假</SelectItem>
                  <SelectItem value="2">病假</SelectItem>
                  <SelectItem value="3">年假</SelectItem>
                  <SelectItem value="4">调休</SelectItem>
                </SelectContent>
              </Select>
              <FormMessage />
            </FormItem>
          )}
        />
        
        <FormField
          control={form.control}
          name="startTime"
          render={({ field }) => (
            <FormItem>
              <FormLabel>开始时间</FormLabel>
              <FormControl>
                <Input type="datetime-local" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        
        <FormField
          control={form.control}
          name="endTime"
          render={({ field }) => (
            <FormItem>
              <FormLabel>结束时间</FormLabel>
              <FormControl>
                <Input type="datetime-local" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        
        <FormField
          control={form.control}
          name="days"
          render={({ field }) => (
            <FormItem>
              <FormLabel>请假天数</FormLabel>
              <FormControl>
                <Input
                  type="number"
                  step="0.5"
                  {...field}
                  onChange={(e) => field.onChange(Number(e.target.value))}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        
        <FormField
          control={form.control}
          name="reason"
          render={({ field }) => (
            <FormItem>
              <FormLabel>请假事由</FormLabel>
              <FormControl>
                <Textarea
                  placeholder="请输入请假事由"
                  className="resize-none"
                  rows={4}
                  {...field}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        
        <div className="flex gap-4">
          <Button type="submit">提交</Button>
          <Button type="button" variant="outline" onClick={() => navigate(-1)}>
            取消
          </Button>
        </div>
      </form>
    </Form>
  )
}
```

## 5. shadcn/ui 组件安装

### 5.1 初始化 shadcn/ui

```bash
npx shadcn-ui@latest init
```

### 5.2 添加常用组件

```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add card
npx shadcn-ui@latest add dialog
npx shadcn-ui@latest add form
npx shadcn-ui@latest add input
npx shadcn-ui@latest add label
npx shadcn-ui@latest add select
npx shadcn-ui@latest add table
npx shadcn-ui@latest add toast
npx shadcn-ui@latest add textarea
npx shadcn-ui@latest add badge
npx shadcn-ui@latest add dropdown-menu
```

## 6. 组件开发

### 6.1 状态徽章组件 (components/StatusBadge/StatusBadge.tsx)

```typescript
import { Badge } from '@/components/ui/badge'

interface StatusBadgeProps {
  status: number
}

const statusConfig: Record<number, { label: string; variant: any }> = {
  0: { label: '草稿', variant: 'secondary' },
  1: { label: '待审批', variant: 'default' },
  2: { label: '审批中', variant: 'default' },
  3: { label: '已通过', variant: 'success' },
  4: { label: '已拒绝', variant: 'destructive' },
  5: { label: '已撤回', variant: 'secondary' }
}

export default function StatusBadge({ status }: StatusBadgeProps) {
  const config = statusConfig[status] || { label: '未知', variant: 'secondary' }
  
  return (
    <Badge variant={config.variant}>
      {config.label}
    </Badge>
  )
}
```

## 7. 开发规范

### 7.1 命名规范
- **组件名**：PascalCase，如 `UserList.tsx`
- **文件名**：PascalCase（组件）或 camelCase（工具函数）
- **变量名**：camelCase，如 `userData`
- **常量**：UPPER_SNAKE_CASE，如 `API_BASE_URL`

### 7.2 代码组织
- 使用函数组件和 Hooks
- 逻辑复用使用自定义 Hooks
- 组件职责单一
- 使用 TypeScript 严格模式

### 7.3 样式规范
- 优先使用 Tailwind CSS 实用类
- 复杂样式使用 `cn()` 工具函数合并
- 遵循 shadcn/ui 的设计系统

## 8. 构建与部署

### 8.1 开发环境运行
```bash
npm install
npm run dev
```

### 8.2 生产环境打包
```bash
npm run build
```

### 8.3 Nginx 部署配置
```nginx
server {
    listen 80;
    server_name approval.yourcompany.com;
    
    root /var/www/approval-frontend/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

> 📅 **文档版本**：v1.1  
> 📝 **最后更新**：2026-01-14  
> 👤 **编写人**：前端开发工程师  
> 🔄 **变更说明**：更新为 React + Tailwind + shadcn/ui 技术栈
